# Default AXIOM hyperparameters (shared across all games)
# Per-game configs override these values.

# Environment
steps: 10000
seeds: 10

# sMM
max_slots: 16
alpha_smm: 1.0

# iMM
max_types: 10
alpha_imm: 1.0

# tMM
max_modes: 32
state_dim: 8
alpha_tmm: 1.0

# rMM
max_rmm_components: 256
alpha_rmm: 1.0

# BMR
bmr_interval: 500
bmr_max_pairs: 2000

# Planning
num_actions: 5
planning_horizon: 8
num_rollouts: 128
utility_weight: 1.0
info_gain_weight: 1.0

# Misc
seed: 0
